// main2.dart
import 'package:flutter/material.dart';

class Main2Page extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Main 2 - Página Secundaria')),
      body: Center(
        child: Text('¡Has llegado a Main 2!'),
      ),
    );
  }
}
